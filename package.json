{
  "name": "tobeatraveller-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,client -c blue,green \"npm run dev --prefix api\" \"npm run dev --prefix client\"",
    "build": "npm test --prefix api && npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
